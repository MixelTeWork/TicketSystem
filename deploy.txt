---- Deploy ----
// git clone -b gh-pages https://github.com/MixelTeWork/TicketSystem.git ticketsystem
git clone -b gh-pages --single-branch https://github.com/MixelTeWork/TicketSystem.git --depth 1 ticketsystem
pip3.9 install -r requirements.txt
python3.9 -m alembic upgrade head
python3.9 -m bafser init_db_values
python3.9 -m bafser change_user_password admin
---
Source code: /home/platformevent/TicketSystem
Working directory: /home/platformevent/TicketSystem
wsgi.py: 
from main import app as application
---------
---- /Deploy ----

---- Update ----
create backup:
	db
	images
	fonts
zip -r images.zip images
zip -r fonts.zip fonts
zip -r logs.zip logs

cd TicketSystem
git restore .
git pull

python3.9 -m alembic upgrade head
python3.9 -m bafser update_roles_permissions

---- /Update ----

backup db
mysqldump -u ticketsystem -h ticketsystem.mysql.pythonanywhere-services.com --set-gtid-purged=OFF --no-tablespaces 'ticketsystem$default'  > db-backup.sql

restore db
source db-backup.sql

restore files
unzip images.zip -d .
unzip fonts.zip -d .